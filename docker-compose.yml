version: "3.8"

services:
  app:
    build: .
    image: hot-updater-bun
    ports:
      - "9292:9292"
    volumes:
      - ./assets:/app/assets
    environment:
      - NODE_ENV=production
      - PORT=9292
      - UPLOADS_DIR=assets/uploads
      - DB_PATH=assets/app.db